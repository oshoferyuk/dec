import { PROPERTIES_SYMBOL } from './types';

export function renderComponent(C: any){
    for(let i = 0; i < c[PROPERTIES_SYMBOL]; i++){
        
    }
}

function <T, U> test: boolean {
    return true;
}

<T, U> arrowF